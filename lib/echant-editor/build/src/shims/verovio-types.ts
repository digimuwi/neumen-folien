/** Stands in for the `verovio` package root, which eChant imports for types
 *  only. Nothing here is used at runtime. */

export type VerovioModule = unknown;
export type VerovioOptions = Record<string, unknown>;
